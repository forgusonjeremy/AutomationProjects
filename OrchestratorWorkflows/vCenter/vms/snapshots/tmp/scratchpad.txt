cloudConfig: |
  #cloud-config
  users:
    - name: sshuser
      shell: /bin/bash
      groups: wheel
      sudo: ["ALL=(ALL) NOPASSWD:ALL"]
      lock_passwd: true
      ssh_authorized_keys:
        - ${input.sshKey}