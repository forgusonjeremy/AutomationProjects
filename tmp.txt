# newest configure-component pods, any state
kubectl get pods -n vmsp-platform | grep -iE 'configure-component|deploy' | sort -k5

# events from the last few minutes
kubectl get events -n vmsp-platform --sort-by=.lastTimestamp | tail -30


kubectl logs -n vmsp-platform <newest-error-pod> --all-containers --tail=60 | grep -iE 'error|fail|not enough|exit code|status code|wrong resource'