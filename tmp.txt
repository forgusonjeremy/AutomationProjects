# the deploy is driven by VCF Ops MSR — find the failing pod/job
kubectl get pods -A | grep -iE 'vrni|netops|ops-for-net|opsnet|platform'

# pending pods + reason
kubectl get pods -A --field-selector status.phase=Pending
kubectl get events -A --sort-by=.lastTimestamp | tail -40