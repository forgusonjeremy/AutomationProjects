kubectl get kcp -A
kubectl get machines -A -o wide
kubectl get machines -A | grep -iv running        # the non-Running ones
kubectl describe machine cvsd26vcfmgt-8lmtt-lh556 -n vmsp-platform | tail -40

# CAPI controller — why the roll is stalled
kubectl logs -n vmsp-platform deploy/capi-kubeadm-control-plane-controller-manager --tail=80 | grep -iE 'error|fail|wait|stuck|etcd'

# is it an etcd-health gate? KCP blocks rollout if etcd quorum isn't healthy
kubectl logs -n vmsp-platform vmsp-etcd-0 --tail=40