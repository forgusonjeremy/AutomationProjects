---
- name: dynamic inventory of ESXi Hosts for each Cluster
  hosts: all
  gather_facts: false

  vars:
    # var_list_vm:
    #     - 'esocdowvxvcs.pilot.dev'
    #     - 'cvsdans11.pilot.dev'
    #     - 'cvsdans12.pilot.dev'
    #     - 'cvsdans13.pilot.dev'
    #     - 'cvsdans14.pilot.dev'
    #     - cvsdgit11.pilot.dev
    #     - cvsdgit12.pilot.dev
    # var_test_port:
    #     - 22
    #     - 443
    #     - 80

  tasks:
  - name: sleep
    ansible.builtin.shell: sleep 60000

  - name: Gather info about the cluster 
    include_tasks: tasks/ports_check.yml
    with_items: "{{ var_list_vm }}"
    loop_control:
      loop_var: port_task_item