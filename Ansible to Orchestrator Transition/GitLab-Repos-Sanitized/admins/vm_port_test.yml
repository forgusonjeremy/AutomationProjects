---
- name: port test from the inventory list
  hosts: localhost

  vars:
    # var_list_vm:
    #     - 'esocdowvxvcs.pilot.dev'
    #     - 'cvsdans11.pilot.dev'
    #     - 'cvsdans12.pilot.dev'
    #     - 'cvsdans13.pilot.dev'
    #     - 'cvsdans14.pilot.dev'
    #     - cvsdgit11.pilot.dev
    #     - cvsdgit12.pilot.dev
    # var_test_port:
    #     - 22
    #     - 443
    #     - 80

  tasks:
  # - name: Gather info about the cluster 
  #   include_tasks: tasks/ping_check.yml
  #   with_items: "{{ var_list_vm }}"

  - name: Gather info about the cluster 
    include_tasks: tasks/ports_check.yml
    with_items: "{{ var_list_vm }}"
    loop_control:
      loop_var: port_task_item