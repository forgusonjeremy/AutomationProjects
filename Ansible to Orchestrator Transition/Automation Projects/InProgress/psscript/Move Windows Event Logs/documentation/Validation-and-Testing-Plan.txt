═══════════════════════════════════════════════════════════════════════════════
VALIDATION AND TESTING PLAN
Windows Archive Log Management — VCF Orchestrator Phase 1
Updated after cvs_functions.ps1 source review
═══════════════════════════════════════════════════════════════════════════════

───────────────────────────────────────────────────────────────────────────────
PHASE A — ENVIRONMENT PRE-CHECKS (Before deploying any vRO content)
───────────────────────────────────────────────────────────────────────────────

A1. Confirm PS host is fully configured and registered in vRO
    The Windows PowerShell host must be configured and added to VCF Orchestrator
    before any workflow in this package can be run.

    See: PS-Host-Build-Guide.txt — complete end-to-end configuration guide.

    Key requirements confirmed before proceeding:
      [ ] WinRM configured with HTTPS listener on port 5986
      [ ] HTTPS certificate meets one of the following conditions:
            a) Signed by a CA already trusted by VCF Orchestrator, OR
            b) Self-signed / internal CA certificate imported into the
               VCF Orchestrator trust store BEFORE the PS host was added
               (see PS-Host-Build-Guide.txt Part 2, Step 2.2)
      [ ] PS host added to vRO via "Add a PowerShell host" workflow
          (Library → PowerShell → Configuration) using HTTPS / Kerberos
      [ ] PS host visible in vRO Inventory → PowerShell
      [ ] Smoke test passed: "Invoke a PowerShell script" with Write-Host succeeds

    IMPORTANT — Certificate must be imported before adding the PS host:
    When HTTPS is selected in the "Add a PowerShell host" workflow, vRO
    validates the certificate against its trust store immediately.  The workflow
    does not offer an option to accept or bypass an untrusted certificate.
    If the certificate is not trusted, the workflow will fail with an SSL error.
    Import the certificate first (Step 2.2 in the build guide), then add the host.

A2. Confirm PowerShellRemotePSObject.getRootObject() is available
    Location: vRO UI → Administration → Scripting API Browser
              → Plugin: PowerShell → Type: PowerShellRemotePSObject
    Confirm:
      [ ] getRootObject() method exists and is callable
    Note: getOutputLine(), getErrorLine(), getExitCode() do NOT exist on this
    type in the installed plugin version.  parseScriptOutput.js uses
    getRootObject() exclusively — no changes needed if getRootObject() is present.

A3. (No longer applicable) PowerShell:PowerShellHost FQDN property name
    This check was only required by the former Move-ArchivedLogs-LocalHost
    workflow, which derived the target server FQDN from psHost.name.  That
    workflow has been removed.  Move-ArchivedLogs-ByADGroup resolves all target
    servers from Active Directory inside the script (Get-ListOfServers-ByCN);
    no FQDN is read from the psHost object.  No action required.

A4. Confirm cvs_functions.ps1 script parameter names
    Script has been reviewed.  The following are confirmed from the param() block.
    Verify the deployed script on the PS host matches:
      [x] -Action              confirmed in ValidateSet
      [x] -DomainName          confirmed
      [x] -FileShareTarget     confirmed
      [x] -SecurityGroup_CN    confirmed — NOTE underscore (not -SecurityGroupCN)
      [x] -UNC_SharePath       confirmed — NOTE underscore (not -UNCSharePath)
      [x] -OlderThanDays       confirmed
      [x] -WhatIf              confirmed

A5. Confirm cvs_functions.ps1 Action values
    Both action values used by this package are confirmed present in the
    deployed script:
      [x] move-archived-logs-ByCN         (Move-ArchivedLogs-ByADGroup)
      [x] Delete-OldFiles-UNC-Share       (Remove-OldFiles-UNCShare)

    NOTE: This package makes NO changes to cvs_functions.ps1.  Both invoked
    -Action values already exist in the deployed script.

A6. Confirm script path on PS host
      [ ] Script exists at the path configured in defaultScriptPath
      Command: Test-Path 'C:\PSO\Scripts\cvs_functions.ps1'
      Expected: True
      If False: update defaultScriptPath in the Config Element to match actual path.

A7. Confirm service account — UNC read access
    Run from PS host under the vRO service account context:
      Test-Path '\\targetserver\C$\Windows\System32\winevt\Logs'
    Expected: True
    If False: review PS host service account permissions on target servers.

A8. Confirm service account — UNC write access
    Run from PS host under the vRO service account context:
      New-Item -Path '\\fileserver\mdcarchivelog$\Windows\test-write-check' -ItemType Directory -Force
      Remove-Item -Path '\\fileserver\mdcarchivelog$\Windows\test-write-check' -Force
    Expected: Both commands succeed without errors.

A9. Confirm Kerberos constrained delegation (double-hop)
    The PS host reads from \\targetserver\C$\... and writes to \\fileserver\share$\...
    This is a double-hop.  The PS host machine account or service account must be
    trusted for constrained delegation to the file server SPN.
    Check: AD Users and Computers → PS host account → Delegation tab
      [ ] "Trust this computer for delegation to specified services only" is set
      [ ] File server CIFS SPN is listed in the allowed services
    If not configured: engage AD team before running live workflows.
    See also: PS-Host-Build-Guide.txt Step 1.6.

A10. Confirm RSAT AD Tools on PS host
    Run on PS host:
      Get-Module -ListAvailable ActiveDirectory
    Expected: Module is listed.
    If not found: install RSAT AD Tools on PS host before running
    the Move-ArchivedLogs-ByADGroup workflow.
    See also: PS-Host-Build-Guide.txt Step 1.1.

A11. Confirm Remove-OldFiles-UNCShare report-only behaviour (WhatIf)
    RESOLVED (see Change-Register.md, change S-1): Remove-OldFiles-UNCPath now
    supports a -ReportOnly parameter.  The Delete-OldFiles-UNC-Share switch case
    maps WhatIf='yes' to -ReportOnly $true (lists candidate files, deletes
    nothing, no prompt) and WhatIf='no' to -Force $true (live delete, no prompt).
    The former interactive Read-Host prompt has been removed.

    Confirm the FIX IS DEPLOYED before relying on WhatIf='yes' from vRO:
      [ ] The deployed cvs_functions.ps1 contains the -ReportOnly parameter on
          Remove-OldFiles-UNCPath:
            Get-Content 'C:\PSO\Scripts\cvs_functions.ps1' | Select-String 'ReportOnly'
            # Expected: matches found (param declaration + switch-case usage)
      [ ] WhatIf='yes' from vRO completes non-interactively and lists candidate
          files without deleting (see test D3)

    If the deployed script does NOT contain -ReportOnly, it is the older version:
    deploy the updated script first, or use WhatIf='no' only.

───────────────────────────────────────────────────────────────────────────────
PHASE B — vRO CONTENT DEPLOYMENT CHECKS
───────────────────────────────────────────────────────────────────────────────

B1. Configuration Element
    [ ] VCF/WindowsLogManagement/WindowsLogManagement-Config exists
    [ ] All four attributes present with correct types and values:
          defaultScriptPath        (string)
          defaultFileShareTarget   (string)
          defaultDomainName        (string)
          defaultLogRetentionDays  (number)
    [ ] defaultScriptPath matches actual script path confirmed in A6
    [ ] defaultFileShareTarget is reachable from PS host

B2. Actions deployed
    Module: broadcom.pso.vc.vm.guestOps.files.windows.logs
    [ ] buildMoveByADGroupInvocation       return type: string
    [ ] buildRemoveFilesInvocation         return type: string
    [ ] parseScriptOutput                  return type: Properties

B3. Workflows deployed
    Folder: PSO >> VC >> VM >> GuestOps >> Files >> Windows >> Logs
    [ ] Move-ArchivedLogs-ByADGroup
    [ ] Remove-OldFiles-UNCShare

B4. OOTB workflow available
    [ ] Library/PowerShell/Invoke a PowerShell script is present in vRO library
    [ ] Output binding confirms it returns PowerShellRemotePSObject

B5. Input bindings — Config Element defaults
    [ ] Move-ArchivedLogs-ByADGroup:      scriptPath ← defaultScriptPath
    [ ] Move-ArchivedLogs-ByADGroup:      domainName ← defaultDomainName
    [ ] Move-ArchivedLogs-ByADGroup:      fileShareTarget ← defaultFileShareTarget
    [ ] Remove-OldFiles-UNCShare:         scriptPath ← defaultScriptPath
    [ ] Remove-OldFiles-UNCShare:         olderThanDays ← defaultLogRetentionDays

───────────────────────────────────────────────────────────────────────────────
PHASE C — UNIT TESTS (Actions — via vRO scripting console or test workflow)
───────────────────────────────────────────────────────────────────────────────

C1. buildMoveByADGroupInvocation — valid input
    Input:  scriptPath='C:\PSO\Scripts\cvs_functions.ps1'
            adGroup='WinLogServers'
            domainName='corp.local'
            fileShareTarget='\\fileserver\share$\Windows'
    Expected return:
      & "C:\PSO\Scripts\cvs_functions.ps1" -Action 'move-archived-logs-ByCN' -SecurityGroup_CN 'WinLogServers' -DomainName 'corp.local' -FileShareTarget '\\fileserver\share$\Windows'
    NOTE: parameter is -SecurityGroup_CN with underscore — not -SecurityGroupCN

C2. buildMoveByADGroupInvocation — empty scriptPath
    Input:  scriptPath='' (empty string)
    Expected: throws Error containing "scriptPath is required"

C3. buildMoveByADGroupInvocation — empty adGroup
    Input:  adGroup='' (empty string), other inputs valid
    Expected: throws Error containing "adGroup is required"

C4. buildMoveByADGroupInvocation — empty domainName
    Input:  domainName='' (empty string), other inputs valid
    Expected: throws Error containing "domainName is required"

C5. buildRemoveFilesInvocation — whatIf=no
    Input:  scriptPath='C:\PSO\Scripts\cvs_functions.ps1'
            uncSharePath='\\fileserver\share$\Windows'
            olderThanDays=370
            whatIf='no'
    Expected return:
      & "C:\PSO\Scripts\cvs_functions.ps1" -Action 'Delete-OldFiles-UNC-Share' -UNC_SharePath '\\fileserver\share$\Windows' -OlderThanDays 370 -WhatIf 'no'
    NOTE: parameter is -UNC_SharePath with underscore — not -UNCSharePath

C6. buildRemoveFilesInvocation — whatIf=yes
    Input:  same as C5 but whatIf='yes'
    Expected return: same string with -WhatIf 'yes'
    Expected log:    System.log noting report-only mode
    NOTE: report-only behaviour at run time requires the updated cvs_functions.ps1
    (-ReportOnly parameter); see A11 and Change-Register.md change S-1.

C7. buildRemoveFilesInvocation — invalid olderThanDays
    Input:  olderThanDays=0
    Expected: throws Error containing "must be an integer >= 1"

C8. buildRemoveFilesInvocation — invalid whatIf
    Input:  whatIf='maybe'
    Expected: throws Error containing "must be 'yes' or 'no'"

C9. parseScriptOutput — getRootObject() against a benign script
    Run the OOTB "Invoke a PowerShell script" workflow with script: Write-Host 'test output'
    Pass the resulting PowerShellRemotePSObject to parseScriptOutput.
    Check vRO execution log for:
      [ ] getRootObject() returns a string containing "test output"
      [ ] No exceptions thrown
      [ ] parsedResult Properties returned with keys: success, outputText, errorLines
      [ ] success = true (no "Error:" in output)

C10. parseScriptOutput — error line detection
    Run the OOTB PS workflow with script:
      Write-Host 'Info: starting'
      Write-Host 'Error: something failed'
      Write-Host 'Info: continuing'
    Pass result to parseScriptOutput with executionContext='test'.
    Expected:
      [ ] success = false
      [ ] errorLines contains the "Error: something failed" line
      [ ] outputText contains all three lines

───────────────────────────────────────────────────────────────────────────────
PHASE D — WORKFLOW INTEGRATION TESTS (Non-production targets)
───────────────────────────────────────────────────────────────────────────────

D1. Move-ArchivedLogs-ByADGroup — safe run
    Pre-condition: Use a small non-critical AD group (1-2 test servers), at least
      one of which has an Archive*.evtx file in C$\Windows\System32\winevt\Logs.
    Run with:  adGroup=<test group identifier>, domainName=<domain>,
               fileShareTarget=<non-prod test share>, psHost=<PS host>
    Expected:
      [ ] Workflow reaches End - Completed Successfully or End - Completed with Errors
      [ ] vRO execution log shows per-server "Info: <server> - moving archived files" lines
      [ ] Archive*.evtx file(s) moved to <fileShareTarget>\<server-short-name>\
      [ ] vRO execution log shows the invocationString with -SecurityGroup_CN (underscore)
      [ ] Only enabled computer members of the group are processed (recursive expansion)
      [ ] No unexpected side effects on servers outside the test group

D2. Remove-OldFiles-UNCShare — preview before live deletion
    With the -ReportOnly fix deployed (A11), preview via the workflow itself:
      1. Run the Remove-OldFiles-UNCShare workflow with whatIf='yes' (see D3) and
         review the "[ReportOnly] WouldDelete" lines.  Confirm the list is
         acceptable.  (Or run the same -Action manually on the PS host with
         -WhatIf 'yes' if preferred.)
      2. Only then run the workflow with whatIf='no' against a non-production
         share with known old test files.
    Expected from vRO run (whatIf='no'):
      [ ] Workflow reaches End - Completed Successfully
      [ ] executionSuccess = true
      [ ] Test files older than threshold are deleted
      [ ] Deletion Summary in vRO log (from Write-Host in Remove-OldFiles-UNCPath end block)
      [ ] No files outside olderThanDays threshold are deleted

D3. Remove-OldFiles-UNCShare — whatIf='yes' report-only verification
    Pre-condition: updated cvs_functions.ps1 (-ReportOnly) deployed (see A11).
    Run with whatIf='yes' against a non-production share with known old test files:
      [ ] Workflow completes non-interactively (no blocking, no prompt)
      [ ] vRO log lists "[ReportOnly] WouldDelete: <file>" lines for aged files
      [ ] NO files are actually deleted from the share
      [ ] Deletion Summary reports "Total files deleted: 0"

───────────────────────────────────────────────────────────────────────────────
PHASE E — SUCCESS CRITERIA
───────────────────────────────────────────────────────────────────────────────

[ ] PS host configured per PS-Host-Build-Guide.txt and registered in vRO
[ ] PS host certificate trusted by vRO (CA-signed or manually imported)
[ ] PS host smoke test passes before any log management workflow is run
[ ] All pre-checks (Phase A) pass or have documented workarounds
[ ] A11 (WhatIf report-only): updated cvs_functions.ps1 (-ReportOnly) deployed and WhatIf='yes' verified report-only (D3)
[ ] Both workflows deploy without import errors
[ ] Config Element attributes load correctly as workflow defaults in custom form
[ ] All three actions execute without runtime errors against valid inputs
[ ] Both build* actions return correctly formatted invocation strings
[ ] Invocation strings use -SecurityGroup_CN and -UNC_SharePath (underscores) — confirmed in logs
[ ] parseScriptOutput returns Properties with keys: success, outputText, errorLines
[ ] parseScriptOutput correctly detects "Error:" lines as success=false
[ ] Move-ArchivedLogs-ByADGroup processes all enabled group members (recursively) correctly
[ ] Move-ArchivedLogs-ByADGroup moves Archive*.evtx files to correct per-server destination
[ ] Remove-OldFiles-UNCShare whatIf='no' deletes correct files on non-prod test share
[ ] All workflow executions produce meaningful log output in vRO execution history

───────────────────────────────────────────────────────────────────────────────
ROLLBACK AND RECOVERY
───────────────────────────────────────────────────────────────────────────────

Item                      Rollback Approach
────────────────────────  ───────────────────────────────────────────────────────────────────────────
cvs_functions.ps1         No changes made by this package — both invoked -Action values already
                          exist in the deployed script.  No rollback needed for script.

Moved .evtx files         Files are moved, not deleted.  If a move places files at the wrong
                          destination, they can be manually moved back.
                          The script creates per-server subfolders; match server short name to
                          identify correct destination.

Deleted files (Remove)    No automated recovery — deletion is permanent.
                          Mitigation: always run script manually with WhatIf='yes' first to
                          preview scope before running vRO workflow with WhatIf='no'.

vRO content               All items are new (no modifications to existing vRO content).
                          Rollback = delete the two workflows, three actions, and Config Element.
                          The OOTB "Invoke a PowerShell script" workflow is not modified.

Config Element            Delete or update attribute values to revert defaults.
                          No production impact until workflows are run.

PS host registration      Remove the PS host from vRO Inventory → PowerShell if needed.
                          This does not affect the Windows Server configuration.
                          The Windows Server WinRM configuration can be reverted by:
                            Remove-WSManInstance winrm/config/listener
                              -SelectorSet @{Address="*";Transport="HTTPS"}
                          This removes the HTTPS listener only; HTTP listener is unaffected.
